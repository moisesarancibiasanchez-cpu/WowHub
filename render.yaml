databases:           # ← NUEVO: clave top-level para PostgreSQL
  - name: wowhub
    plan: starter
    region: oregon


services:            # ← solo para web/worker/cron
  - type: web
    name: wowhub-api
    runtime: docker
    envVars:
      - key: DATABASE_URL
        fromDatabase:    # ← cambiado de fromService
          name: wowhub
          property: connectionString